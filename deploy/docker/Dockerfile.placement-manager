FROM rust:1.82-bookworm AS build
WORKDIR /src
COPY . .
RUN cargo build --release -p placement-manager

FROM debian:bookworm-slim
COPY --from=build /src/target/release/placement-manager /usr/local/bin/placement-manager
ENTRYPOINT ["/usr/local/bin/placement-manager"]
